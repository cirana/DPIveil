{
  "name": "Default",
  "description": "HTTPS profile using TLS ClientHello fragmentation.",
  "filter": "outbound and tcp and tcp.DstPort == 443",
  "strategy": "tls_client_hello_fragment",
  "strategy_options": {
    "first_chunk_size": 32
  }
}
