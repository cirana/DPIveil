{
  "name": "Default",
  "description": "Blockcheck-proven Discord TLS desync profile using a Zapret-compatible split.",
  "filter": "(outbound and tcp and tcp.DstPort == 443) or (inbound and tcp and tcp.SrcPort == 443 and (tcp.Syn or tcp.Rst))",
  "strategy": "zapret_compat",
  "strategy_options": {
    "mode": "multisplit",
    "split_pos": 2,
    "fake_ttl": 1,
    "target_domains": ["discord.com"]
  }
}
